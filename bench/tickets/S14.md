# S14 — compose: project network created then reported not found (BuildKit conformance)
- source: docker-client conformance (BuildKit-default mode) · step: compose-up · class: test-fixture · status: closed · first seen: 2026-08-26
- failure: `docker compose --project-name <p> --file tests/fixtures/real-app/compose.yml up --detach` against a fresh socket reports `network: not found <p>_frontend` or `<p>_backend`.
- regression window: BuildKit conformance was 85/85 at 65063708 and 84/85 at the local merge 06a27a16 (S3, S10, S11, S12). Classic mode still passes 85/85, so the failure is mode-specific or timing-dependent.
- cause: this fixture explicitly declares both project networks as `external: true`; Compose therefore never creates them. The bare reproduction is expected to fail on an empty state and fails identically after the code changes. It is not a slirp-helper reaper issue. The initial “containers created” output is Compose’s parallel create phase; the subsequent start correctly requires the missing external network.
- fix: no product reaper change is required for this fixture contract. The conformance harness creates `ferrocrate-conformance-scoreboard-compose_frontend` and `_backend` before its Compose-up row, which is the required setup for the fixture. The daemon’s durable-record invariants are separately covered by `slirp_reaper_leaves_a_recorded_container_helper_alone`, the fresh create/list Docker API regression, and concurrent multi-network Docker API creation.
- repro: start the daemon with --docker-compat, `export DOCKER_HOST=unix:///run/user/1000/ferrocrate.sock`, then the compose command above from tests/fixtures/real-app.
- passing evidence: classic conformance passed 85/85 and BuildKit-default conformance passed 85/85 on 2026-08-26; both include the externally provisioned multi-network Compose-up row. The direct bare command was reproduced before and after the fixes and correctly fails until its two external networks are provisioned.
