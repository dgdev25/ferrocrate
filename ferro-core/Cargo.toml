[package]
name = "ferro-core"
version = "0.1.0"
edition = "2024"

[dependencies]
nix = { version = "0.30", features = ["process", "sched", "signal", "user"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"

[dev-dependencies]
tempfile = "3.13"
