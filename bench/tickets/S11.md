# S11 — compose: `build: .` string form rejected
- app: battleships · step: compose-up · class: product · status: closed (3ca34ff2)· first seen: 2026-08-26
- command: `ferro-cli compose up -d` with `services.battleships.build: .`
- Docker: accepts (Compose spec: build may be a string context path). Ferrocrate: `compose parse error: services.battleships.build: invalid type: string ".", expected struct Build`.
- fix: accept string and mapping forms for `build` (and other spec fields with short forms: `ports`, `volumes`, `environment` list/map).

## Verification
Verified 2026-08-26: with the image present, `ferro-cli compose up -d` in /data/dev/battleships (whose compose.yaml uses the `build: .` short form) exits 0, the app answers HTTP 200 on its published port, and `compose down -v` exits 0. BuildKit-mode conformance compose-up passes 85/85 on three consecutive runs.
Gate: classic conformance 85/85, BuildKit conformance 85/85, workspace tests pass. Pushed on origin/main at 72c1a33d.
