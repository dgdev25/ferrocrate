# S163 — Closed BuildKit fixes lacked dockerd-containerd-worker parity verification
- suite: buildkit-dockerfile (post-fix verification coverage gap; product failures are tracked by S156, S158, and S160) · class: test · status: closed 2026-08-30 — parity verification executed and recorded: 2026-08-30 17:50 parity run (bench/results/2026-08-30/suite-buildkit-dockerfile-ferrocrate.jsonl at 95350738: 387 pass / 19 fail / 14 skip); remaining 19 failures are owned by S210 (entitlement/network variants, S159's dockerd-worker scope) and S158 (LLB remainder) · first seen: 2026-08-28
- command: `GO_TEST_TIMEOUT=0 SUITE_SRC=/data/dev/bench-suites bash bench/suites/run-suite.sh buildkit-dockerfile --engine ferrocrate`, examining `worker=dockerd-containerd` records.
- finding: S141, S142, S70, and S15 correctly verified their repaired builtin/dockerd paths, but their written verification did not explicitly cover `worker=dockerd-containerd`. The 2026-08-28 full run reached that variant after the fixes and retained failures in the same broad families: `TestErrorsSourceMap` syntax diagnostics, Dockerfile-check warning assertions, client gateway callback solves, and stage/COPY cases.
- history: this is not a regression. The earlier full record committed as `3e507bb8` (run 2026-08-27T18:37Z, head `cd1fcdc8`) already contains failing `worker=dockerd-containerd` records for `TestErrorsSourceMap/*/invalidsyntax`, `TestConsistentInstructionCasing/*/warntype=progress|unmarshal`, `TestSourceDateEpoch*`, and `TestMultiStageImplicitFrom`; there is no saved passing record for those disputed subcases. Thus the gap is specifically that the closed fixes were not reverified on this worker after landing, not that the worker was literally never exercised.
- environment: Docker can execute this worker on this host: the Docker-oracle result records passes for the corresponding source-map syntax, warning, and multi-stage cases. `docker version` succeeds and `unshare -Ur` succeeds. This is not a root/containerd boundary.
- scope: add focused post-fix assertions for `worker=dockerd-containerd` wherever a BuildKit repair closes a worker-sensitive failure family, and keep the product work separated: S160 owns Dockerfile-check rule semantics, S158 owns unsupported client callback solves, and S156 owns FileSync-dependent stage/COPY contexts.
- test: run a focused matrix for `worker=dockerd-containerd` and both frontends covering one source-map syntax error, one lint warning through progress and unmarshal, one client gateway solve, and one staged COPY; record explicit pass/fail evidence in the owning ticket before closure.

## Evidence from the 2026-08-28 full run

- Source maps: six invalid-Dockerfile subcases pass on each frontend, but `invalidsyntax` fails: builtin receives no error and client has no range for line 1. This is a syntax/diagnostic semantic case, not S141's former missing-source transport failure.
- Lint delivery: warning results now arrive in several cases, but rule counts/content differ (`expected N, actual M`), matching S160 rather than S142's former absent result/progress transport.
- Gateway and stages: client callback failures retain `buildkit solve: method not implemented` (S158); FileSync/stage/COPY failures are covered by S156. The fixed S70 `Inputs` route is not the old blanket unimplemented transport failure.

## 2026-08-29 lab verification

Ran `GO_TEST_TIMEOUT=0 SUITE_SRC=/data/dev/bench-suites bash bench/suites/run-suite.sh buildkit-dockerfile --engine ferrocrate` with this clone's release binary. The upstream harness labelled 677 records `worker=dockerd-containerd`: 177 passed, 491 failed, and 9 skipped. `TestErrorsSourceMap/*/invalidsyntax` failed on both builtin and client; `TestConsistentInstructionCasing` and `TestCopyIgnoredFiles` contained both passes and failures, with the remaining warning-count/content failures owned by S160.

This lab does not start a real dockerd/containerd worker. Its harness runs inside `unshare -Ur` and its `dockerd` shim proxies the labelled worker's Docker and BuildKit traffic to Ferrocrate's compatibility socket. Therefore these are honest labelled-worker compatibility observations, not proof of parity with a genuine dockerd-containerd worker. Status remains open until a focused four-case matrix can run against that real worker or the lab gains one.
