# Paths removed from the public snapshot (one per line, relative to repo root,
# directories end with /). Read by scripts/export-public-snapshot.sh.
# Internal docs live in the gitignored docs/internal/ (ADR-018) and never
# enter the archive; this list covers what is still tracked on main.
ROADMAP-CLOSEOUT.md
.superdesign/
# The export script embeds the private path table it scrubs; ADR-018 describes it.
scripts/export-public-snapshot.sh
