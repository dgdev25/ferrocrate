# Dockerfile — generated by Ferrocrate. Edit freely.
FROM @BASE@ AS build
WORKDIR /src
COPY . .
RUN cargo build --release @MAIN@
FROM debian:bookworm-slim
COPY --from=build /src/target/release/ /opt/app/
ENV PORT=@PORT@
EXPOSE @PORT@
CMD ["sh", "-c", "@START@"]
