# S202 — cli: native `ferro-cli create` reports success but the container never exists

- area: ferro-cli create (native path) · class: product · status: open · first seen: 2026-08-29 · severity: high (silent success)
- command: `ferro-cli create --name verify-created alpine:3.20 true` then `ferro-cli ps --all` / `ferro-cli inspect verify-created`.
- Docker: create persists a created-state container visible in ps -a; diff/cp/commit/rm on it all succeed (oracle-verified 2026-08-29).
- Ferrocrate: create prints a container ID and exits 0, but ps --all shows "no entries" and inspect/diff/cp/commit/rm all fail "container not found". The docker-compat socket path creates correctly (conformance rows created-container-* PASS), so the defect is the native CLI path only — likely writing to (or reading from) the wrong store, or not persisting at all.
- repro (2 commands): create as above, then inspect — rc!=0 immediately after rc=0 create.
- test: native create then ps --all must list the container and inspect must return its JSON; then rm must succeed.
- evidence: ferrotest matrix 2026-08-29 rows 125-131, logs 125-131; manual reproduction twice (verify-created, matrix-created) with identical result.
