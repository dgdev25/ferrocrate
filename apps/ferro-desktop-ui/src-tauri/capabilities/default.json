{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the desktop app window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
