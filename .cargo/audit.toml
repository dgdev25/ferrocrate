[advisories]
# Existing time-bounded maintenance exception; owner, dependency path and expiry
# are enforced by scripts/check-advisory-exceptions.py and recorded in the register.
ignore = ["RUSTSEC-2025-0141"]
