You are on a fresh branch from origin/main in a fresh clone of Ferrocrate. Read bench/README.md and bench/tickets/<id>.md. Reproduce the ticket with its exact command before changing code. Fix the product code, add a regression test in the owning crate, run the gate (cargo test --workspace; scripts/verify-no-warnings.sh; scripts/docker-client-conformance.sh in both build modes). Retest with bench/run-app.sh <app> both --skip-clone and paste the step's result line into the ticket; set status closed. Small commits, no docs-only commits, never force-push, one cargo process at a time. Print exactly TICKET-<id>-CLOSED on its own line when the retest is green, or TICKET-<id>-BLOCKED <reason> if it is not.
