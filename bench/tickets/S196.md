# S196 — compose build rejects relative `RUN --mount` target (Kutt regression)
- app: kutt · steps: `compose-up`, `compose-health` · class: product · status: open · first seen: 2026-08-28 · refiled from unmerged triage commit f99663bd 2026-08-29
- command: `cd /data/dev/bench-apps/kutt && ferro-cli compose up -d` through the benchmark adapter.
- Docker: the fresh oracle builds and starts the Compose project.
- Ferrocrate: `invalid Dockerfile: cache mount target must be an absolute non-parent path` during Compose build.
- cause: the relative-target resolution delivered by `d61fe09e`/`afee6c97` is no longer applied before cache-mount validation for this Dockerfile's workdir-relative target.
- scope: Dockerfile `RUN --mount=type=bind|cache,target=<relative>` used through Compose builds; the health row is a direct cascade of the failed build.
- test: build Kutt's Compose Dockerfile plus focused bind and cache relative-target fixtures under a non-root `WORKDIR`, asserting Docker-compatible path resolution and rejection only for escaping parents.
- proof: this is a regression, not an assumed repeat of closed S92. The prior committed app run at `fd9f2fd8` advanced beyond mount validation and instead failed later while pulling `node:22-alpine`, proving the Kutt Compose step had accepted the relative mount target. The fresh `ade7ba3b` Ferrocrate result now has the original target error while Docker passes.
- relation: distinct from S162 (RUN mount/resource options not applied); this is a regression in relative-target resolution through Compose builds, pinned by proof below.
