# S187 — compose: successful Kutt startup never advances from migration to the HTTP server
- app: kutt · step: compose-health · class: product · status: closed 2026-08-30 — not reproducible: retested twice at current HEAD (compose-wait stream, 2026-08-29; matrix retest, 2026-08-30) with the Kutt health sequence passing; if it recurs, file a fresh ticket with the new reproducer · first seen: 2026-08-28
- command: `cd /data/dev/bench-apps/kutt && /data/dev/ferrocrate-app-bench-refresh3/bench/ferro-adapter.sh compose up -d --build`, then probe `curl --resolve localhost:34990:127.0.0.1 http://localhost:34990/`.
- Docker: the Compose project becomes reachable on port 34990 and the matrix `compose-health` step passes.
- Ferrocrate: `compose up -d --build` exits 0 and `compose ps` lists `server`, but the full 120-second health probe fails. A direct isolated reproduction connects then receives `Recv failure: Connection reset by peer`; the service log reaches only `> kutt@3.2.6 migrate` / `> knex migrate:latest`, never the `npm start` server output.
- cause: the Compose-launched `CMD npm run migrate && npm start` lifecycle does not leave a serving Kutt process after the migration stage. This is distinct from S155: the base-image/build acquisition succeeds and `compose up` returns 0.
- scope: Compose services whose image command chains initialization and a long-running server with shell `&&`; a successful `up` can falsely report readiness while the published port resets clients.
- test: bring up a fixture with `CMD npm run migrate && npm start` (or an equivalent short init plus HTTP server), assert the init completes, the server remains running, and a published-port HTTP probe succeeds; preserve the Kutt Compose reproduction.
- proof: fresh isolated 15-app matrix at head `d2c13996` records Kutt `compose-up` pass and `compose-health` failure at 11:37Z. A second isolated `bash bench/run-app.sh kutt ferrocrate --skip-clone` at 11:54Z reproduced the 120985-ms health failure; direct Compose inspection reproduced the connection reset and migration-only log.
- retest 2026-08-29 at 681a250e (isolated runtime, ferro-cli daemon in docker-compat mode): the Kutt image built with `ferro-cli build -t kutt-local .` (152 MB, exit 0), then `docker compose -p s187k2 up -d` over a compose file carrying the same `npm run migrate && npm start` entrypoint, the sqlite named volume at `/var/lib/kutt`, and `ports: "36003:3000"`. The log advances past `> knex migrate:latest` (`Batch 1 run: 10 migrations`) into `> kutt@3.2.6 start` / `Ready on http://localhost:3000`, the container stays `running` past 40 s, and `curl http://127.0.0.1:36003/` answers HTTP 302. The shell-`&&` lifecycle itself is also healthy: an alpine service with `command: sh -c "sleep 2 && sleep 300"` stays running past the hand-off. No runtime code between `d2c13996` and `681a250e` touches that lifecycle, so the pilot failure most likely came from the build/first-boot environment rather than the migration-to-server hand-off; rerun the matrix before closing. Note: in this environment the buildkit-h2 build path reports `Image … Built` without persisting the image to the image store, and the following run then fails with a registry 401 — the native `ferro-cli build` path persists the same Dockerfile correctly.
