# S3 — build: failing step output hidden, wrong message
- app: battleships · step: build · class: product · status: closed (f381dc47)· first seen: 2026-08-26
- command: any Dockerfile whose RUN fails
- Docker: prints the step log and `exit code: 1`. Ferrocrate: `remote context request returned HTTP 400: {"message":"invalid Dockerfile: RUN failed with status exit status: 1"}` with no step output; the build is local.
- fix: stream step stdout/stderr, name the failing step, drop "remote context request" for local builds.

## Verification
Verified 2026-08-26: a Dockerfile with `RUN echo visible-step-output && false` prints `visible-step-output` before `error: invalid Dockerfile: RUN /bin/sh -c echo visible-step-output && false failed with status exit status: 1`. The failing step is named and its output is shown; the message no longer says "remote context request" for a local build.
Gate: classic conformance 85/85, BuildKit conformance 85/85, workspace tests pass. Pushed on origin/main at 72c1a33d.
