Add one application to the bench: pick the next entry from docs/testing/REAL-APP-BENCH-PLAN.md section 4 that has no bench/apps/<app>/manifest.yaml, verify its licence is permissive, write the manifest, run bench/run-app.sh <app> both, classify every Ferrocrate failure with bench/triage.md, open a ticket file for each product failure (bench/tickets/T<yyyymmdd>-<n>.md with the fields in bench/tickets/README.md), run python3 bench/scoreboard.py, commit manifest + results + tickets + scoreboard. Print exactly APP-<app>-ADDED on its own line when committed.
