services:
  api:
    build: .
    command: ["node", "-e", "require('http').createServer((_, r) => r.end('ok')).listen(8080)"]
    ports:
      - "31080:8080"
  helper:
    image: node:22-slim
    command: ["node", "-e", "setInterval(() => {}, 1000)"]
