# S198 — build: 2.6G real-project context rejected with "archive total size exceeds transfer limit"

- app: cession (`/data/dev/ferrotest/cession`, 2.6G context) · step: build · class: product · status: open · first seen: 2026-08-29
- command: `cd /data/dev/ferrotest/cession && ferro-cli build -t ferrotest-cession:latest .`
- Docker: builds successfully (verified 2026-08-29 against Docker 29.7.2; full build completes).
- Ferrocrate: `error: archive total size exceeds transfer limit` after ~22s.
- cause: the build path enforces a fixed archive/transfer size limit Docker does not have (or has far higher); find the limit constant and the Docker behaviour (docker streams the context tar with no fixed cap).
- test: build a generated context larger than the limit (e.g. 3G of zeros in a file) and assert the build proceeds as Docker does.
- evidence: ferrotest matrix run 2026-08-29, results.tsv row 14, log 014; docker oracle build succeeded in the same directory.
