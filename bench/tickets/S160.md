# S160 — BuildKit Dockerfile lint rules are delivered but do not match Dockerfile-check semantics
- suite: buildkit-dockerfile (176 failing warning assertions after the S142 delivery fix) · class: product · status: closed 2026-08-30 — all lint families green in the 2026-08-30 17:50 parity run (bench/results/2026-08-30/suite-buildkit-dockerfile-ferrocrate.jsonl at 95350738: 387 pass / 19 fail / 14 skip); TestRuleCheckOption/TestCopyIgnoredFiles/TestUnmatchedVars/TestUndeclaredArg/checkLinterWarnings variants all pass · first seen: 2026-08-28
- command: `GO_TEST_TIMEOUT=0 SUITE_SRC=/data/dev/bench-suites bash bench/suites/run-suite.sh buildkit-dockerfile --engine ferrocrate`.
- Docker: Dockerfile checks emit exactly the rule diagnostics selected by the Dockerfile and `--check` options. Ferrocrate now reaches both progress and unmarshalled warning paths, but emits zero warnings where one or more are required, emits warnings when zero are requested, or produces a different message (for example no undefined-variable suggestion). The result is `expected N, actual M` in `dockerfile_check_test.go`, not the S142 missing-result signature.
- cause: the local Dockerfile parser/validator does not implement BuildKit's lint-rule evaluation and option filtering with Docker-compatible semantics. S142's formerly missing transport has exposed this separate semantic gap.
- scope: rule selection plus warning content/count for casing, ignored-copy, undefined-argument/variable, JSON-args, stage-name, EXPOSE, legacy-format, and related Dockerfile checks.
- test: execute one enabled and one disabled stable rule through both progress and unmarshal forms; assert identical rule name, detail, location, URL, and count to the Docker oracle.
- evidence: fresh failures across `TestRuleCheckOption` (48), `TestCopyIgnoredFiles` (20), `TestUnmatchedVars` (14), `TestUndeclaredArg` (13), and the other `checkLinterWarnings` variants.

2026-08-30 convergence boundary: Docker/buildx oracle comparisons and exact progress/unmarshal regressions now cover `TestRuleCheckOption`, `TestCopyIgnoredFiles` (including context-root and Dockerfile-copy behavior), `TestUnmatchedVars`, `TestUndeclaredArg`, and `TestJSONArgsRecommended` (including custom `SHELL` inheritance and the canonical URL). The earlier premise still holds: Dockerfile directives and frontend `BUILDKIT_DOCKERFILE_CHECK` options select and skip rules, with frontend options taking precedence; `error=true` now also matches the oracle's warning-plus-error behavior.

This ticket remains open. A fresh remaining-only, all-worker/all-frontend run at `aaff797b` produced 80 pass, 395 fail, and 2 skip at the suite-subtest level. Restricting the evidence to failing `warntype=progress|unmarshal` leaves gives 321 assertions: 61 visible lint-semantic mismatches and 260 assertions obscured by the audited filesync/RST solve flakes. The 61 visible mismatches were `LegacyKeyValueFormat` 8, `MultipleInstructionsDisallowed` 8, `RedundantTargetPlatform` 7, `InvalidDefaultArgInFrom` 6, `WarningsBeforeError` 5, `ExposeProtoCasing` 4, `FromPlatformFlagConstDisallowed` 4, `StageName` 4, `WorkdirRelativePath` 4, `MaintainerDeprecated` 3, `DefinitionDescription` 2, `ExposeInvalidFormat` 2, `SecretsUsedInArgOrEnv` 2, `DuplicateStageName` 1, and `NoEmptyContinuation` 1. `ConsistentInstructionCasing`, `ReservedStageName`, and `AllTargetUnmarshal` had no visible semantic assertion in this run, but all of their failing warning leaves were transport-obscured, so they are not claimed converged. The verified remaining count is therefore bounded at 61–321 warning assertions until a flake-free census can distinguish the 260 obscured leaves.
