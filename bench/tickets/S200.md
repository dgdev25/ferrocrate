# S200 — buildkit compose build fails on 86M real context: filesync "too_many_data_frames" connection error

- app: battleships (`/data/dev/ferrotest/battleships`, 86M context) · step: compose build via docker-compat · class: product · status: closed (20ec2602 bounds BuildKit h2 receive bursts; generated 86M Compose fixture passes) · first seen: 2026-08-29
- command: `cd /data/dev/ferrotest/battleships && DOCKER_HOST=unix://<ferrocrate-compat-sock> docker compose build` (also `docker compose up` which builds first).
- Docker: builds successfully (oracle-verified 2026-08-29, "Image battleships-battleships Built").
- Ferrocrate: `failed to solve: Internal: buildkit filesync: receive failed: connection error detected: detected excessive load generating behavior (b"too_many_data_frames")` after ~1s, preceded by an h2 GOAWAY graceful shutdown. Related to S156 (session file-sync) but a distinct symptom: an HTTP/2 frame/load shield kills the filesync stream; a small conformance context passes, an 86M real context does not.
- cause: likely an h2 server setting (max frames/data-window) or an explicit load shield in the docker-compat daemon's gRPC/http2 layer closing streams that send many DATA frames.
- test: start the compat daemon, run `docker compose build` on a context of ~86M (copy battleships or generate), assert the build completes as Docker does.
- evidence: ferrotest matrix 2026-08-29 rows 1+3, logs 001/003; docker oracle build succeeded in the same directory.
