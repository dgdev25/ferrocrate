# S8 — doctor: orphaned directories with no cleanup command
- app: n/a · step: doctor · class: product · status: open · first seen: 2026-08-26
- command: `ferro-cli doctor` after lab runs
- Ferrocrate: `[fail] state_store: 2 volume data directory(ies) without store records; 3 container directory(ies) without store records` and a hint to "inspect them, then remove with volume rm after recreating a record".
- fix: `system prune --orphans` (or accept orphan IDs in `volume rm` / `rm`); doctor names the command.
