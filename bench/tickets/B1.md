# B1 — bench: Docker-oracle failures that are manifest or template problems (not product)
- class: bench · status: closed · first seen: 2026-08-26 · owner: GLM (bench-run session)
- All six apps pass on Docker. Per-app fixes, all confined to manifests, templates, or bench scripts:
- gin-basic: go template now builds with `-buildvcs=false` (go build in a build stage cannot stamp VCS info from a mounted/dirty context) and runs on `alpine:3.22` instead of distroless (matrix `exec sh` needs a shell).
- actix-basics: manifest switched from `-p basics` to `-p docker_sample`; that workspace package binds `0.0.0.0:8080`, while every `basics/*` example binds `127.0.0.1` and can never be reached through a published port.
- flask-tutorial: two layers. (1) manifest start sets `FLASK_APP=flaskr` inline and passes `--app flaskr`, because run-app.sh's env-map parser splits on spaces so `env: {FLASK_APP: flaskr}` reached the container as an empty value. (2) run-app.sh escaped `&` before sed substitution of `@START@` — a literal `&&` in any start command previously expanded to `@START@@START@`.
- docker-todo: its Traefik proxy routes on `Host(\`localhost\`)`; matrix.sh compose-health probe now uses `curl --resolve localhost:<port>:127.0.0.1` so the Host header matches.
- docker-flask: web service sits behind `profiles=[web]`, enabled via COMPOSE_PROFILES in .env by setup; upstream ports entry interpolates `${PORT}`, which collides with the bench-exported PORT variable, pinned to `8000:8000` via setup sed; health path corrected `/up` → `/up/` (308 redirect).
- kutt: `.example.env` ships an empty JWT_SECRET (app refuses to boot), filled by setup; compose hardcodes host port 3000 which this host holds, remapped to 34990 and matched by compose_port. Note: the remap cannot use `$PORT` — manifest setup runs before run-app.sh exports it.
- Done when all six pass on Docker: gin-basic 52/52, actix-basics 52/52, flask-tutorial 57/57, docker-todo 52/52, kutt 19+33skip/0fail, docker-flask 19+33skip/0fail. Scoreboard regenerated: 15 apps, 0 product failures.
