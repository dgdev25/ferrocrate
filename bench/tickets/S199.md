# S199 — cli: `ferro-cli logs` missing `--tail` (and `--since`/`--until`/`--timestamps`) options

- area: ferro-cli logs · class: product (CLI parity) · status: closed · first seen: 2026-08-29
- command: `ferro-cli logs --tail 5 <container>`.
- Docker: `docker logs --tail N` (plus --since, --until, --timestamps, --details) are standard options; `--tail 5` works.
- Ferrocrate: argument parse error rc=2 — logs supports only `--format` and `-f/--follow`.
- test: run a container emitting numbered lines; `logs --tail 2` must show exactly the last 2 lines, matching docker output.
- evidence: ferrotest matrix 2026-08-29 row 23 log 023; `ferro-cli logs --help` shows the option set.

Fix: added Docker-compatible `--tail`, `--since`, `--until`, and `--timestamps` selectors, with a numbered-log regression test for `--tail 2`.
