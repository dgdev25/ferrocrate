# S210 — buildkit entitlement/network variant failures and SourceDateEpoch HTTP cases (19 remaining in parity run)

- suite: buildkit-dockerfile · class: product · status: open · first seen: 2026-08-30
- context: the 2026-08-30 17:50 parity run (387 pass / 19 fail / 14 skip at 95350738) converged 472 previously-failing cases. The 19 that remain fall in two families:
  1. Entitlement granted/denied variants across workers (14): TestRunSecurityDefault/Insecure/Sandbox (granted variants on dockerd and dockerd-containerd workers), TestRunDefaultNetwork/TestRunGlobalNetwork/TestRunHostNetwork/TestRunNoNetwork (granted+denied on both workers), TestInsecureDevicesWhitelist (granted, both frontends). stderr shows the suite's dockerd sandbox output — the entitlement-routed adapter landed with S159 must not be forwarding per-worker entitlement config for these shapes, or the daemon gate rejects what it should grant.
  2. SourceDateEpoch reproducibility with HTTP/git sources (5): TestWorkdirSourceDateEpochReproducible, TestSourceDateEpochNamedContextHTTPArchive/HTTPLastModified (builtin+client), and the git-variant of source_date_epoch_test.
- S159's closure covered the product gate and adapter routing; these variant failures are its uncovered scope, filed here rather than reopening (the status ledger must never move backward).
- test: re-run the failing subtests via SUITE_SRC=/data/dev/bench-suites bash bench/suites/run-suite.sh buildkit-dockerfile --engine ferrocrate and assert the 19 pass.
- evidence: bench/results/2026-08-30/suite-buildkit-dockerfile-ferrocrate.jsonl, failures grouped in S163's closure note.
