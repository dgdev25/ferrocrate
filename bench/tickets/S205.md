# S205 — build: cession build fails at frontend tsc step — path Docker resolves is missing in Ferrocrate build

- app: cession (`/data/dev/ferrotest/cession`) · step: build (after S198 fix lets the 2.6G context transfer) · class: product · status: open · first seen: 2026-08-29
- command: `cd /data/dev/ferrotest/cession && ferro-cli build -t verify-cession:latest .`
- Docker: full build succeeds rc=0 (oracle-verified twice 2026-08-29).
- Ferrocrate: build proceeds past context transfer (S198 fixed) but the project's own build step fails: `> cession-frontend@0.0.0 build` → `error TS5083: Cannot read file '/app/src/frontend/tsconfig.json'` (twice — two RUN steps). The file exists in the context; Docker's equivalent step finds it. Suspect COPY relative-path resolution, WORKDIR-relative execution, or .dockerignore interpretation diverging from Docker in the classic builder.
- test: minimal Dockerfile `FROM alpine` + `WORKDIR /app` + `COPY . /app` + `RUN test -f /app/src/frontend/tsconfig.json` over the cession tree shape (nested dir containing a config) must succeed; compare against docker.
- evidence: /tmp/verify-cession-build.log 2026-08-29 18:36–19:48; docker oracle rc=0 /tmp/oracle-cession2.log.
