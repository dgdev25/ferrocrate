# S209 — regression resurfacing: two port-publishing compose services fail "publish requires --network bridge" via compat socket

- suite: ferrotest verification · class: product (regression of S189 shape) · status: closed · first seen: 2026-08-30
- command: compat daemon (debug binary at HEAD ~e4d5457c+) + a compose.yaml with TWO services each publishing a distinct port (`docker compose up -d`).
- Docker: both services start and serve.
- Ferrocrate: `Error response from daemon: run: publish requires --network bridge` on the second container's create — the exact S189 symptom, which was verified fixed on 2026-08-29 (three-service project green through compose-net2's fix). Resurfaces at current HEAD, so a later merge (S172 ephemeral-port work or s181-land's rootless changes are the candidates) regressed the joined-network publish guard again.
- test: two-service compose project, both publishing, up via compat socket — both must reach running (this was S189's test; extend/re-run it and find which merge dropped the coverage or broke the guard).
- evidence: /tmp/s181-verify2.log 2026-08-30 13:2x; single-service project works, proving the guard is the failure point, not networking generally.

Fixed 2026-08-30: the two-service file reproduced through `docker compose` and an isolated Docker-compatible daemon at HEAD, then also reproduced at `874d65a4` (the S189 merge). The claimed post-S189 regression had no later regressing commit: S172 (`681a250e`) and `s181-land` (`e4d5457c`) are excluded. S189's `9fb84dbc` fix carried bridge-join provenance only through native `ferro-cli compose`; its compat `/containers/{id}/start` caller still passed `false`. The surviving three-service test (`rootless_compose_implicit_network_publishes_every_port_publishing_service`) was both native-path and opt-in, so neither its service count nor normal workspace runs covered the daemon path.

The guard now derives publish permission from the resolved durable bridge-network record, accepting its rootless `container:pid:<leader>` follower while leaving raw namespace joins and non-bridge modes rejected. Regression coverage: `compat_named_bridge_follower_reaches_run_validation_with_published_port` covers provenance and negative modes at the guard; `docker_compat_two_compose_services_publish_on_the_same_named_bridge` runs two Compose-shaped services with distinct published ports through the compat daemon and requires both to reach `running`.
