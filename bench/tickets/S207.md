# S207 — build: intermittent "layer decompression failed: failed to decompress gzip layer" during pull-under-build

- app: cession (2.6G build pulling node/nginx/python base layers) · step: build · class: product · status: open · first seen: 2026-08-30
- command: `ferro-cli build -t verify-cession:latest .` in /data/dev/ferrotest/cession (isolated fresh FERROCRATE_HOME).
- Docker: build succeeds (oracle rc=0 twice).
- Ferrocrate: one run of three failed mid-build with `invalid Dockerfile: layer decompression failed: failed to decompress gzip layer`; immediate retry with identical inputs succeeded (rc=0, full build completed 2026-08-30 02:33). Non-deterministic — likely a truncated/racy layer fetch or partial-read treated as a corrupt stream.
- test: pull a known multi-layer image on a bandwidth-limited/throttled loop N times; assert every attempt either succeeds or retries the layer; a decompress error must not surface to the user.
- evidence: /tmp/verify-cession3.log (failed run), /tmp/verify-cession4.log (succeeded retry), 2026-08-30.
