# S182 — BuildKit build output is missing the `naming to ... done` line

- suite: compose-e2e · class: product defect (compat contract) · status: open · first seen: 2026-08-28
- command: `docker compose run --build simple` (TestRunBuildOnceNoDeps); the test asserts the output matches `naming to .*e2e-run-build-once-no-deps-simple.* done`
- Docker: buildx/buildkit prints `#N naming to <ref> done` as part of the export stage.
- Ferrocrate: the build runs (the image and container exist afterwards) but the export/naming progress line is absent from the stream. The same test output also shows a `chown(...)` invalid-argument error worth checking while fixing.
- impact: 1 failure, plus any client that parses build progress.
- regression tests: run a BuildKit build and assert the progress stream contains a `naming to <ref> done` line.
