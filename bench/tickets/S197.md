# S197 — missing regression tests for the S178/S185 compose fixes (compose-cmd2 stream)

- area: compose volume/build paths · class: test · status: open · first seen: 2026-08-29
- The compose-cmd2 stream's prompt required one commit per fix with a regression test from the ticket's reproducer. The fixes (S178 project bind volumes with options, S185 inherited service builds) merged without any test additions — only src changes in ferro-cli/src/main.rs and ferro-compose/src/lib.rs.
- risk: the fixed paths can regress silently; the tickets are closed so no open work tracks them.
- test: S178 — a compose project with a bind-specified project volume must create/resolve it as a bind, not look up a named volume. S185 — a service with neither image nor explicit build that inherits its image from another service's build must be accepted and start. Both as integration tests in ferro-cli/tests or ferro-compose/tests using the tickets' reproducing sequences.
- evidence: compose-cmd2 branch diffstat (5 files, no test files); merge 12:33 2026-08-29.
