# S204 — cli: native `ferro-cli build` rejects `--build-arg`

- area: ferro-cli build · class: product (CLI parity) · status: open · first seen: 2026-08-29
- command: `ferro-cli build --dockerfile Dockerfile --tag local/s174-direct:latest --build-arg RELEASE=2026.08 .`
- Docker: `docker build --build-arg K=V` is standard; the ARG reaches the Dockerfile build environment.
- Ferrocrate: `error: unexpected argument '--build-arg' found` rc=2 — the flag does not exist on native build (usage shows --build-context but no --build-arg). The compose path forwards build args (S173/S174 fixed that path only).
- test: Dockerfile `FROM scratch` + `ARG RELEASE` + `LABEL org.ferrocrate.s174.release=$RELEASE`; build with --build-arg RELEASE=x; image inspect must show the label value. Also covers the S197-required direct-path regression test.
- evidence: found by the s197-tests stream 2026-08-29 while writing the S174 regression test; exact reproducer recorded in S197.md's blocked note.
