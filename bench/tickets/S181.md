# S181 — `nginx:alpine` service container exits with code 1 during `up -d --wait`

- suite: compose-e2e · class: product defect · status: open 2026-08-30 — implementation and IPC regression fix landed; the installed profile text now contains `/usr/bin/bwrap ix,`, but the loaded host policy still transitions the helper to `bwrap//&unpriv_bwrap` and denies nginx `CAP_CHOWN`, so effective host-profile activation/correction is the single remaining real-image verification item · first seen: 2026-08-28
- command: `docker compose up -d --wait` over the suite's start-stop fixture (two services, both `image: nginx:alpine`) (TestStartAlreadyRunning, TestStopAlreadyStopped, TestStartStopMultipleServices)
- Docker: both containers stay running and `--wait` exits 0.
- Ferrocrate: the second-started `simple` container reaches `Started`, then reports `container ...-simple-1 exited (1)`; `--wait` exits 1. The sibling `another` container of the same image keeps running, so the failure is per-container, not per-image.
- impact: 3 failures directly; `TestUnusedMissingEnvFile` and `TestLocalComposeVolume/access_bind_mount_data` (`service "X" is not running`) are likely the same cause and were not counted.
- regression tests: start two containers of `nginx:alpine` in one project, assert both stay running past `--wait`; check `container logs`/exit code capture for the exited one so the reason is visible.
- diagnosis (2026-08-29, reproduction on this bench host at 681a250e): every `nginx:alpine` container under the rootless launcher exits with `nginx: [emerg] chown("/var/cache/nginx/client_temp", 101) failed` — `container logs` captures the reason. The "per-container, not per-image" reading in the record above does not hold: in an isolated reproduction both project containers exit with the same chown failure; the surviving sibling only appears when the second container dies earlier, at start, with `shared network namespace pid:<leader pid> does not exist` because the leader already exited. Two stacked causes, both required for the fix:
  1. The workload user namespace maps only uid 0 (`uid_map` reads `0 1000 1`, written by the `unshare --user --map-root-user --net` launcher in ferro-core/src/runtime.rs), so uid 101 is unmapped and `chown(…, 101)` returns `EINVAL`. Docker-rootless maps the caller's subordinate range (`0 1000 1` plus `1 100000 65535`); `unshare --user --map-root-user --map-auto` (util-linux ≥ 2.40) produces exactly that shape and `chown 101` then succeeds inside the namespace. `--map-auto` cannot simply be added to the launcher: util-linux forks when it installs the multi-entry map and the parent exits, which breaks the launcher contract that the spawned process stays the workload (every rootless start then fails with `rootless workload exited before creating its network namespace`, measured 2026-08-29). The mapping has to be installed on the stopped launcher instead — for example `newuidmap`/`newgidmap` on the launcher pid before release, alongside the existing authorized `apply_user_namespace_mappings` path in ferro-core/src/rootless.rs.
  2. This host's distro AppArmor profile (`/etc/apparmor.d/bwrap-userns-restrict`) attaches to `/usr/bin/bwrap` and stacks `unpriv_bwrap` onto the workload, which denies `CAP_CHOWN` inside the user namespace (audit: `apparmor="DENIED" operation="capable" profile="unpriv_bwrap" comm="chown" capname="chown"`). With the mapping fixed, `chown(…, 101)` becomes the right target uid but is then denied with `EPERM`, so nginx still exits 1. Root cause of the denial is the profile transition at `/usr/bin/bwrap`, so the remedy is profile/packaging-side — the shipped `packaging/apparmor/usr.local.bin.ferrocrate` profile would need to keep the bwrap helper inside the FerroCrate profile (explicit `ix` rule) the way the design note in docs/design/apparmor-rootless-userns.md assumes — not a runtime-code change.
- blocker verification (2026-08-30, `d439ded7`): `grep bwrap /etc/apparmor.d/usr.local.bin.ferrocrate` finds the packaged `ix` rule, so the full `nginx:alpine` Compose E2E was run. It reaches nginx, proving namespace setup is no longer the blocker, but `cat /proc/self/attr/current` changes from `unconfined//&usr.local.bin.ferrocrate (unconfined)` to `bwrap//&unpriv_bwrap (enforce)` across `/usr/bin/bwrap`. The kernel audit records `profile="unpriv_bwrap" ... capname="chown"`, and both-image `compose up -d --wait` consequently still fails at `chown("/var/cache/nginx/client_temp", 101)` with `EPERM`. Leave S181 open only until the installed/loaded host policy actually prevents that transition; immediately rerun `rootless_compose_nginx_services_wait_running` after the profile is corrected or activated.
- S181C mapping verification (2026-08-30 22:27–22:34 BST, clone release binary): the Docker-compat daemon plus `docker compose up -d` path now puts a live workload in the external uid map `0 1000 1; 1 100000 65535`. The mapped launcher and workload report `CapEff=000001ffffffffff`, including `CAP_CHOWN`; the intermediate bwrap reports `CapEff=0`, as expected, before the workload regains capabilities in the mapped namespace. The new rootless integration regression asserts both the exact `RootlessConfig` uid map and the `CAP_CHOWN` bit and passes. The exact single-service `nginx:alpine` rerun nevertheless exited 1 at `chown("/var/cache/nginx/client_temp", 101)`; fresh kernel audit events 429–430 identify `profile="unpriv_bwrap"` denying `dac_override` and `chown`, and the live workload label was `bwrap//&unpriv_bwrap (enforce)` even though `/etc/apparmor.d/usr.local.bin.ferrocrate` contains `/usr/bin/bwrap ix,`. Therefore the launcher mapping/capability defect is fixed, but S181 remains open because this host's effective loaded AppArmor policy contradicts the required unconfined premise and the nginx end-to-end acceptance has not passed.
- S181C upstream-shape verification (2026-08-30 22:38–22:58 BST, freshly rebuilt clone release binary): the initial exact Compose reproduction still exited 1 at nginx `chown(…, 101)`. The mapping regression was then proved red/green: with only the new all-rootfs mapping selector disabled, the live workload map collapsed to `1000 1000 1` and the test failed; restored, it passed with `CAP_CHOWN`. Docker's rootless documentation and RootlessKit's `newugidmapArgsFromSubIDRanges` implementation show that rootless mode maps container 0 to the caller and appends the complete subordinate allocation from container ID 1, independently of network setup. The implementation now matches that exact shape: live launcher/workload `uid_map` is `0 1000 1; 1 100000 65536`, launcher/workload `CapEff=000001ffffffffff`, and bwrap's intermediate `CapEff=0`. The nginx acceptance run still cannot close S181: both a clone daemon under the installed FerroCrate profile and `/usr/local/bin/ferrocrate` itself transitioned `/usr/bin/bwrap` to `bwrap (enforce)` and the workload to `bwrap//&unpriv_bwrap (enforce)`; audit records 450–451 denied `dac_override` and `chown`. S181 remains open until the effective host policy stops that transition and an nginx service stays running end to end.
- S181C final verification (2026-08-30 23:51–2026-08-31 00:0x BST, freshly rebuilt clone release binary and isolated runtime): the exact single-service `nginx:alpine` Compose reproduction exited 1 at `chown("/var/cache/nginx/client_temp", 101)` through the clone daemon. A long-running probe on the same Docker-compatible path showed launcher and workload `uid_map` `0 1000 1; 1 100000 65536` and `CapEff=000001ffffffffff`, while intermediate bwrap had `CapEff=0`; the focused rootless integration regression passed. The loaded host policy still labeled bwrap `bwrap (enforce)` and the workload `bwrap//&unpriv_bwrap (enforce)`, and fresh audit events 517–518 denied `dac_override` and `chown`. The installed FerroCrate profile text contains `/usr/bin/bwrap ix,`, but `aa-exec -p usr.local.bin.ferrocrate` independently reproduced the same transition; this account cannot reload a corrected host profile because `sudo -n` requires interactive authentication. Software gates passed: `cargo test --workspace`, `scripts/verify-no-warnings.sh`, Docker client conformance in BuildKit and classic modes (`85/85` each), and `scripts/merge-gate.sh` (`2354` workspace tests, `MERGE GATE: PASS`). S181 remains open because the required nginx end-to-end acceptance did not pass on the effective host policy.
- s181d evidence (2026-08-31 06:5x, main 51693d1c release binary, bwrap profile disabled on host): the docker-compat daemon + compose up path still launches the workload in the HOST user namespace — fresh nginx master (started this run, not an orphan) shows uid_map `0 0 4294967295` and CapEff 0, chown(…,101) EPERM. The merged subid-mapping regression passes under cargo test but the daemon-launched path does not exercise it — find the divergence between the rootless path the regression covers and the daemon's container-launch path, and make the daemon path install the same subordinate mapping. Note: /data/dev/ferrocrate vs /home/USER/dev/ferrocrate are separate checkouts; all evidence here is from /data/dev/ferrocrate.
