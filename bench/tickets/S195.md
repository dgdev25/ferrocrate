# S195 — property_image_reference.rs proptests a local parser copy, not the production image-reference parser

- area: workspace property tests (`tests/property_image_reference.rs`) · class: test · status: closed · first seen: 2026-08-29
- fix: Replaced the local parser copy with `ferro_core::registry::parse_image_reference`; round-trip assertions now verify stable production canonicalization.
- command: `cargo test --test property_image_reference` (workspace root).
- Expected: property tests exercise the production parser used by the runtime, so a regression in the real parser fails the property suite.
- Actual: the file defines its own `parse_image_reference` in-file and proptests that copy (roundtrip, never-panics on `\PC*`, name extraction, empty/shell-char rejection; 9 proptests at 1000 cases each). The production validator lives elsewhere (ferro-core exposes `ferro_core::image_security::validate_image_reference`, which `tests/security_input_validation.rs` correctly targets); the full reference parser used by image handling is not what this suite fuzzes.
- cause: the test file predates or bypasses the production parser; a local re-implementation was written instead of importing the real one.
- scope: the suite can pass while the production parser regresses (or vice versa) — the two implementations can drift silently. Weakens the guarantee the property suite appears to provide.
- test: replace the in-file parser with the production parser's API (import it), keep the same properties; if the production API shape does not support what the properties need, that gap is itself the finding.
- evidence: codebase audit 2026-08-29; contrast with tests/security_input_validation.rs, which deliberately targets the production validator.
