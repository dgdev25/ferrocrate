# S203 — cli: `ferro-cli login` missing `-p/--password` and `--password-stdin`

- area: ferro-cli login · class: product (CLI parity) · status: closed · first seen: 2026-08-29
- command: `ferro-cli login -u testuser -p testpass localhost:5005`.
- Docker: `-p/--password`, `--password-stdin` are standard login options.
- Ferrocrate: `error: unexpected argument '-p' found` rc=2 — only --username exists (interactive prompt only, unusable in scripts).
- context: push/pull/tag against a local registry verified working (ferrotest matrix rows 141-144); only the password flag surface is missing.
- test: `login -u u -p p <registry>` succeeds against a test registry; `--password-stdin` reads the password from stdin; password never appears in process args in the --password-stdin path.
- evidence: ferrotest matrix 2026-08-29 rows 140/145, log 140.
- fix: Added `-p`/`--password` and stdin-only password intake for `--password-stdin`.
