# S39 — daemon: system/df, prune, images/search and events endpoints return bare 404
- suite: compose-e2e (tail of the failure list; blocks the bench matrix's extras group too) · class: product · status: open · first seen: 2026-08-27
- command: `curl --unix-socket <sock> ... http://localhost/v1.43/system/df` (likewise `POST /images/prune`, `POST /containers/prune`, `GET /images/search?term=alpine`, `GET /events`)
- Docker: all return real data.
- Ferrocrate: `404 {"message":"not found"}` for each. Verified with curl on 2026-08-27 against release-artifacts/ferro-cli.
- cause: the Docker-compat HTTP server implements no routes for these. Anything that calls them — `docker system df`, prune flows, `docker search`, `docker events` — fails against Ferrocrate.
- scope: five routes: `GET /system/df` (may answer from store statistics), `POST /images/prune`, `POST /containers/prune`, `GET /images/search`, `GET /events`. If a route is declared out of scope it needs a feature-matrix row; per triage rules a missing route with no row is product.
- test: one curl per route asserting a non-404 status and a JSON body of the documented shape.
