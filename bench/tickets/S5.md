# S5 — build: COPY drops a directory that contains a symlink
- app: scratch · step: build · class: product · status: closed · first seen: 2026-08-26
- command: context `d/real.txt`, `d/bin/link -> ../real.txt`; `COPY d /d`; `RUN ls -laR /d`
- Docker: `/d/bin/link` present as a symlink. Ferrocrate: `/d/real.txt` only; `bin/` never arrives, no warning.
- fix: preserve symlinks (relative, absolute, dangling) in the context walk and layer writer.
- fix commit: `a60fed70 fix(build): preserve symlinks in COPY layers`. `add_directory` emitted
  only regular files, so links and empty directories vanished from every generated layer;
  it now writes explicit directory and symlink headers and preserves the link target verbatim
  (relative, absolute, dangling). The context hash walk includes links now too, so retargets
  invalidate cache keys. Regression tests: `copy_preserves_symlinks_and_empty_directories_in_the_layer`,
  `context_hash_includes_symlink_targets` (`ferro-core/src/dockerfile_build.rs`).
- retest app: `bench/apps/scratch` added as the seed-defect fixture. Its Dockerfile asserts the
  link survives with the exact ticket command shape:
  `COPY d /d` + `RUN test -L /d/bin/link && [ "$(readlink /d/bin/link)" = "../real.txt" ]`.
- retest (run 2026-08-26T17:02Z, head 11fb74a8), `bench/run-app.sh scratch both --skip-clone`:

```
{"app":"scratch","engine":"docker","step":"build","status":"pass","exit":0,"ms":303,"stderr_tail":"...","run":"2026-08-26T17:02Z","head":"11fb74a8"}
{"app":"scratch","engine":"ferrocrate","step":"build","status":"pass","exit":0,"ms":104,"stderr_tail":"","run":"2026-08-26T17:02Z","head":"11fb74a8"}
```

scratch on both engines: pass=16 fail=0 skip=31.

Gate notes (2026-08-26):
- `cargo test --workspace`: green (106 suites).
- `scripts/verify-no-warnings.sh`: green.
- `scripts/docker-client-conformance.sh`: fails at exit=2,
  "cold-base BuildKit build did not emit the metadata vertex" — pre-existing on clean HEAD
  (reproduced with a stashed tree and rebuilt binary). Daemon log shows `buildkit control:
  h2 handshake failed: Broken pipe` then a tokio panic dropping a blocking runtime inside an
  async context. Not caused by this ticket; needs its own product ticket for the BuildKit
  progress path.
