# S161 — BuildKit Dockerfile platform and OS-version arguments are not evaluated as BuildKit expressions
- suite: buildkit-dockerfile (at least 16 direct product failures) · class: product · status: open · first seen: 2026-08-28
- command: `GO_TEST_TIMEOUT=0 SUITE_SRC=/data/dev/bench-suites bash bench/suites/run-suite.sh buildkit-dockerfile --engine ferrocrate`.
- Docker: `FROM --platform` and implicit platform arguments are expanded and validated as BuildKit expressions, including an accurate error for undeclared/misspelled variables; multi-platform metadata paths remain representable even when a particular runtime cannot execute every platform. Ferrocrate instead rejects the raw expression as an unsupported instruction, tries to pull malformed names such as `library/base-`, or returns only `linux/amd64 is available` before the expected expression validation.
- cause: platform flags and BuildKit automatic arguments are handled by the local image builder as already-resolved runtime values rather than by a Dockerfile/frontend expression phase; OS-version and multi-platform exporter paths inherit the same gap.
- scope: `FROM --platform`, `BUILDPLATFORM`/`TARGETPLATFORM` and related automatic args, OS-version arguments, and multi-platform OCI/provenance exports.
- test: solve a Dockerfile that uses valid automatic platform args and one that misspells an arg; assert the valid expansion and Docker-compatible invalid-expression diagnostic before platform execution is selected.
- evidence: `TestPlatformArgs*`, `TestPlatformWithOSVersion`, `TestMaintainBaseOSVersion`, `TestMultiNilRefsOCIExporter`, `TestDuplicatePlatformProvenance`, and `TestUndeclaredArg` failures in the fresh run.

## 2026-08-30 boundary after the platform remainder

Status remains **open** after the focused census. The bridge-side portion is
implemented and regression-covered:

- the target platform is resolved before Dockerfile expansion and binds all
  five `TARGET*` arguments, including `TARGETOSVERSION`; explicit build args
  still override the automatic defaults;
- explicit target OS version, cache identity, and matching base-image OS
  version inheritance are preserved in the output image config; and
- gateway results now expose BuildKit's `refs.platforms` mapping and
  platform-scoped `containerimage.config/<id>` metadata. Repeated requests for
  the bridge's one native platform use a `Refs` result and collapse to one
  platform identity. Distinct platform IDs fail closed rather than aliasing
  one classic-builder image to multiple platforms.

This boundary follows BuildKit's contracts at
`2a684cd90798b5cade3cfef31b4981cf33920526`: `defaultArgs` formats the complete
target platform (including OS version) before expression expansion; the image
writer keys config and references by `refs.platforms`; provenance parses that
same mapping and associates attestations with the keyed result.

The S162b forwarder repair (`a53b7e59`) removed the prior accept-loop crash.
Against BuildKit `eece108c`, the unfiltered run reached 383 pass / 35 fail / 17
skip before an unrelated `SOURCE_DATE_EPOCH` integration panic prevented these
late cases from being recorded. A focused run of `TestPlatformWithOSVersion`,
`TestMaintainBaseOSVersion`, `TestMultiNilRefsOCIExporter`,
`TestDuplicatePlatformProvenance`, and `TestUndeclaredArg` recorded 18 pass / 67
fail / 8 skip. The classic dockerd worker skips the first four for missing
multi-platform/OCI/direct-push features; the dockerd-containerd variants reach
the bridge but currently surface `buildkit filesync: close request failed: user
error: inactive stream`, which masks the underlying solve verdict.

Remaining work is therefore narrower and explicit:

1. preserve the solve error across filesync shutdown so focused upstream cases
   report their real product verdict instead of `inactive stream`/`RST_STREAM`;
2. add genuine per-platform execution and image results before claiming the two
   distinct Windows OS-version platforms used by `TestPlatformWithOSVersion`
   and the Windows direct-push path used by `TestMaintainBaseOSVersion`;
3. implement the general multi-reference OCI exporter path required by
   `TestMultiNilRefsOCIExporter` (metadata alone does not create an OCI index);
   and
4. implement attestation object production/routing for
   `TestDuplicatePlatformProvenance` (the platform mapping is now present, but
   no provenance attestation is fabricated by the classic bridge).

Do not close S161 until those focused cases pass on a worker that advertises
their required features. The current result file is
`bench/results/2026-08-30/suite-buildkit-dockerfile-ferrocrate.jsonl`.
