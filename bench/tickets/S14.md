# S14 — compose: project network created then reported not found (BuildKit conformance)
- source: docker-client conformance (BuildKit-default mode) · step: compose-up · class: product · status: open · first seen: 2026-08-26
- failure: `docker compose --project-name <p> --file tests/fixtures/real-app/compose.yml up --detach` against Ferrocrate's docker-compat socket creates the containers, then fails with `Error response from daemon: network: not found <p>_backend`.
- regression window: BuildKit conformance was 85/85 at 65063708 and 84/85 at the local merge 06a27a16 (S3, S10, S11, S12). Classic mode still passes 85/85, so the failure is mode-specific or timing-dependent.
- prime suspect: `2f671f4e fix(core): reap rootless slirp helpers` — 398 changed lines in ferro-core/src/runtime.rs plus container_store.rs, including the network-entry bookkeeping. A reaper that runs while compose is bringing services up can remove or hide a network that compose has just created.
- likely the same class as S13 (create succeeds, list is empty): both are "resource created, then not found", and both regression windows contain a new orphan reaper (S13: 8cf784d2 prune orphaned runtime directories; S14: 2f671f4e reap slirp helpers).
- repro: start the daemon with --docker-compat, `export DOCKER_HOST=unix:///run/user/1000/ferrocrate.sock`, then the compose command above from tests/fixtures/real-app.
- fix: no reaper may remove or hide a resource that has a live store record or an in-flight creation; add a regression test that runs a multi-network compose up through the docker-compat API while the reaper is active.
