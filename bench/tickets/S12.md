# S12 — CLI: missing Docker flag forms
- app: battleships · steps: ps, stats, container-prune, image-prune, compose-down · class: product · status: closed (33293455)· first seen: 2026-08-26
- missing: `ps -a` (only `--all`), `stats --no-stream` (has `--follow`), `container prune -f/--force` and `image prune -f/--force`, `compose down -v/--volumes`, `image rm`. `export -o` exists.
- fix: accept the Docker short and long forms as aliases; keep existing names. Extends S9 (grouped verbs landed in fcf62393).

## Verification
Verified 2026-08-26: `ps -a`, `stats --no-stream <c>`, `container prune -f`, `image prune -f` and `image rm <tag>` all succeed against the release binary.
Gate: classic conformance 85/85, BuildKit conformance 85/85, workspace tests pass. Pushed on origin/main at 72c1a33d.
