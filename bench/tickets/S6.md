# S6 — build: COPY node_modules/<pkg> fails with an unnamed io error
- app: battleships · step: build · class: product · status: open · first seen: 2026-08-26
- command: in /data/dev/battleships: `COPY node_modules/cors /cors` (4 files, no symlinks)
- Docker: passes. Ferrocrate: `HTTP 500: {"message":"io error: No such file or directory (os error 2)"}`; no path named. The same COPY shape works in a small scratch context; `COPY public /public` in the same project works.
- fix: find the failing path (context walk, ignore rules, tree size); every io error in the build path must name the path.
