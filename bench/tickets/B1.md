# B1 — bench: Docker-oracle failures that are manifest or template problems (not product)
- class: bench · status: open · first seen: 2026-08-26 · owner: GLM (bench-run session)
- gin-basic: build fails from the repo root (`go build ./basic`); check go version and the example's imports; may need `GOFLAGS=-mod=mod` or a different example.
- actix-basics: `cargo build --release -p basics` fails in the workspace; pick the right package name from `basics/basics/Cargo.toml` or build in the subdir with a vendored workspace stub.
- flask-tutorial: build passes, health fails; check the start command (`flask init-db && flask run`), the port, and the app's first-request behaviour; capture container logs.
- docker-todo, docker-flask, kutt: compose-health fails; verify `compose_port`, the health path per app, and whether the app service needs more than 120 s or an env file.
- Done when all six pass on Docker (the oracle must be clean before Ferrocrate parity means anything).
