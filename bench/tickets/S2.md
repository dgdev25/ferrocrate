# S2 — build: RUN steps have no network (rootless)
- app: battleships · step: build · class: product · status: closed (dbfdeced; retested: RUN getent + TCP inside a build)· first seen: 2026-08-26 · host: Ubuntu 26.04, kernel 7.0.0-30, rootless
- command: Dockerfile with `RUN getent hosts registry.npmjs.org` (or `RUN npm ci`)
- Docker: resolves and installs. Ferrocrate: inside RUN: no DNS, `/dev/tcp/1.1.1.1/443` fails, `/proc/net/dev` absent; `ferro-cli run --rm node:22-slim npm view ruvector version` works.
- fix: give build steps the same network path as `run` (resolv.conf, /proc, rootless bridge or slirp).
