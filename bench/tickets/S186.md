# S186 — commit: runtime bind and tmpfs mounts are not excluded from committed rootfs
- apps: battleships, flask-tutorial · step: commit (with `rmi-committed` downstream) · class: product · status: open · first seen: 2026-08-28
- command: run the normal app lifecycle through `rename`, then `ferro-cli commit bench-<app>-r bench/<app>:committed` (for example, `bash bench/run-app.sh battleships ferrocrate --skip-clone`).
- Docker: commits the renamed stopped container and the resulting `bench/<app>:committed` image can be removed.
- Ferrocrate: `commit` exits 1 with `commit: bind and tmpfs mounts must be removed before committing the rootfs`; because no committed image is created, the following `rmi-committed` correctly cannot find its target but is only downstream evidence.
- cause: the commit rootfs capture path retains runtime bind/tmpfs mounts rather than excluding or safely materializing them before archiving the container filesystem.
- scope: committing any container that carries the runtime's standard bind or tmpfs mounts; ordinary app lifecycle cleanup cannot create the expected image.
- test: start and stop a container with the normal runtime mount set, rename it, commit it, inspect the committed image, and remove it; assert all operations match Docker and that runtime-only mounts are absent from the image layer.
- proof: fresh isolated 15-app matrix at head `d2c13996`, `bench/results/2026-08-28/battleships-ferrocrate.jsonl` (11:21Z) and `flask-tutorial-ferrocrate.jsonl` (11:32Z), records the identical direct commit error while Docker passes both steps.
