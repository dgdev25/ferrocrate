# S201 — cli: `ferro-cli system` missing `df` and `info` subcommands

- area: ferro-cli system · class: product (CLI parity) · status: open · first seen: 2026-08-29
- command: `ferro-cli system df`, `ferro-cli system info`.
- Docker: `docker system df` (with -v) and `docker system info` are standard; `ferro-cli system` exposes only `prune`.
- Ferrocrate: `error: unrecognized subcommand 'df'` / `'info'` rc=2. (`docker system df/info` through the compat socket works — this is the native surface only.)
- test: `ferro-cli system df` prints the images/containers/volumes/build-cache size table; `system info` prints client+server info like `ferro-cli info`/docker.
- evidence: ferrotest matrix 2026-08-29 rows 49-50; `ferro-cli system --help` shows only prune.
