# S206 — buildkit filesync: "symlinks are not supported" on real context containing node_modules

- app: battleships (`/data/dev/ferrotest/battleships`) · step: compose build via docker-compat · class: product · status: closed · first seen: 2026-08-29
- command: compat daemon + `cd /data/dev/ferrotest/battleships && docker compose build`.
- Docker: builds successfully (oracle-verified 2026-08-29), including the node_modules symlink farm.
- Ferrocrate: after S200's h2 burst fix (verified working — the too_many_data_frames error is gone), filesync now fails at `node_modules/.bin/harness` with `failed to solve: Internal: buildkit filesync: symlinks are not supported: node_modules/.bin/harness`. Real JS projects universally have node_modules/.bin symlinks, so any node project context fails.
- cause: the filesync (session file-sync) implementation rejects symlink directory entries instead of transferring them with BuildKit's semantics (follow/record per upstream: local mounter records symlinks as-is).
- test: generated context containing a relative symlink (like node_modules/.bin/harness → ../harness/bin.js) built via docker compose build through the compat daemon must succeed and the symlink must exist in the build container.
- evidence: /tmp/verify-bs-build.log 2026-08-29 19:50; prior S200 verification run.
- fix: record BuildKit filesync symlink entries with their target strings instead of rejecting them.
