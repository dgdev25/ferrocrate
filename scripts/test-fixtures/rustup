#!/usr/bin/env bash
set -euo pipefail

if [[ "${1:-}" == "target" && "${2:-}" == "list" && "${3:-}" == "--installed" ]]; then
  printf '%s\n' ${FERROCRATE_TEST_RUSTUP_TARGETS-x86_64-unknown-linux-gnu}
  exit 0
fi
echo "unsupported fixture rustup invocation" >&2
exit 2
