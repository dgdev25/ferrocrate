# S27 — compose: every service is refused as an unauthorized child
- apps: all eleven with a compose file · step: compose-up · class: product · status: partial fix — shared resolver now includes all image launch defaults; final qualification remains blocked by an unrelated Docker auto-remove/wait failure and external runtime contamination · first seen: 2026-08-26, present in every run since
- command: `ferro-cli compose up -d --build`
- Docker: brings the stack up. Ferrocrate: `compose partial result: <service> run failed: runtime mutation mediation failed: compose execution request digest does not match authorized child`.
- cause: `ferro-core/src/runtime.rs` computes `run_execution_digest` from the pinned image, command, environment, labels, annotations, healthcheck, restart policy, limits, workdir, user, name, port mappings and the normalized request, then rejects the launch unless it equals the digest compose authorized through `fanout_request_digest_matches`. Compose authorized unresolved image defaults (`workdir: None`, `user: None`, and an incomplete command), while `handle_run` resolved the image command and the runtime later inherited `WorkingDir` and `User`. The check arrived with `d806d82b`.
- fix: Compose resolves the pinned image configuration once after image prerequisites and before child authorization. It binds command, environment, workdir, user, healthcheck, and image-declared volume targets into `normalized_run_execution_digest`, then passes the same `ResolvedRunImageExecution` object and pre-materialized anonymous-volume mounts to `handle_run`. `handle_run`, direct `run`, and Docker create/start use the same override application with caller values taking precedence over image defaults; the launcher consumes the supplied resolved object rather than rebuilding its own view. The comparison remains enforced; a changed command is still rejected.
- to find it: log both digests and their inputs at the point of comparison, then diff the input sets. The likely culprits are fields the runtime fills in after compose authorized the child: values inherited from the image config, a normalized network mode, or a generated container name.
- scale, measured 2026-08-27 with docker/compose's own e2e suite: 270 tests, Docker passes 264, Ferrocrate passes 22. That is 229 product failures against 6 that fail on both engines. The entire fourteen-application bench has 144. This single defect is the largest in the project.
- impact: compose-up fails on 11 of 14 applications, and compose-health, compose-ps and compose-down fail behind it. This is the largest remaining cluster in the bench.
- proof: `scripts/verify-no-warnings.sh` passes after the shared-resolver change. `cargo test -p ferro-cli run_image_resolution_applies_image_defaults_and_cli_overrides_once` proves image `Env` and `Healthcheck` inheritance plus explicit environment and health overrides; `cargo test -p ferro-core compose_run_executor_rejects_a_mismatched_normalized_digest` authorizes `true`, mutates it to `false`, and is rejected. The S27 fixture declares `WORKDIR`, `USER`, and `VOLUME`; its build reaches launch without the compose digest rejection. A fresh `cargo test --workspace` run reaches 83 passing Docker integration tests but reproducibly fails `docker_compat_auto_remove_preserves_wait_exit_result`: the auto-remove worker deletes the record before `/wait?condition=next-exit` reads its persisted exit result (HTTP 404). This is outside the compose digest path and blocks criterion 1.
- suite proof: after merging `origin/main`'s per-repository `-mod=vendor` correction, the compose suite at `03ad3cc` collected real tests. Docker recorded 216 pass / 50 fail / 14 skip (280 records); Ferrocrate recorded 249 pass / 20 fail / 14 skip (283 records). Fifteen Ferrocrate failures passed on Docker, down from the earlier 229-product-failure report. This source revision does not have the requested 270-test shape, so the two record sets remain a qualification observation rather than the final pinned comparison.
- qualification blockers: the BuildKit client-conformance harness stops at its cold-base metadata-vertex assertion, and classic mode is 82/85 rather than 85/85. The fourteen-app scoreboard has not been rerun: its current compose runtime carries a stale `network-cleanup-pending.json` from earlier application work, which cascades unrelated failures. `bwrap=1` and `slirp4netns=3` belong to another agent's `/path/to/ferrocrate-lab/runtimes/fuzz` workload; they must not be killed from this task. The two-service published-port proof also remains blocked by the separate rootless-network failure (`run: publish requires --network bridge` after joining the leader namespace).
